/** COMPOSE — main/sub screens + color math. Body lands in the Compose/Windows
 *  inspector ticket; this file is its mount point. */
export function ComposeTab() {
  return <div className="insp-empty">Compose — main/sub + color math coming soon.</div>;
}
