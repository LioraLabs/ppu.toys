/** MEMORY — VRAM regions + CGRAM ownership. Body lands in the Trace/Memory
 *  inspector ticket; this file is its mount point. */
export function MemoryTab() {
  return <div className="insp-empty">Memory — VRAM + CGRAM ownership coming soon.</div>;
}
