/** WINDOWS — W1/W2 masks + combine logic. Body lands in the Compose/Windows
 *  inspector ticket; this file is its mount point. */
export function WindowsTab() {
  return <div className="insp-empty">Windows — W1/W2 masks coming soon.</div>;
}
