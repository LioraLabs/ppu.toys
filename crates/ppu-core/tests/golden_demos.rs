//! DISABLED by m4/memory (M4 substrate rewrite): the demo goldens drove the two
//! flagship examples over deleted v1 direct-RGBA `Source`s. TODO(m4/demos):
//! rewrite dusk-parallax + mode7-floor onto the indexed-VRAM substrate (via
//! the M4 importer + DSL surface), regenerate
//! tests/fixtures/golden_dusk_parallax.png / golden_mode7_floor.png, and
//! re-enable as the M4 done gate.

#[test]
#[ignore = "v1 direct-RGBA demo goldens; rewritten on the new substrate in m4/demos"]
fn golden_demos_disabled_pending_substrate_demos() {}
