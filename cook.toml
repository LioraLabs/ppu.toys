# Cook module pins — the web/ pipeline runs through the blessed cook_pnpm module
# (one-call workspace surface; see the register block in the Cookfile).
[registry]
indexes = ["https://rocks.usecook.com"]

[modules]
cook_pnpm = "0.6.0-1"
