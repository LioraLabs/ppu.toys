# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

# LOCAL DELTA (PPU-97): its own workspace root, so `cargo test` inside
# vendor/piccolo works without the crate joining the ppu.toys workspace.
[workspace]

# LOCAL DELTA (PPU-97): rustc caps lints for a registry dependency but not for a
# path one, so vendoring surfaces three warning sites upstream never had to fix.
# Both are form lints, never correctness ones, and they are named rather than
# blanket-allowed so nothing else gets a free pass:
#   irrefutable_let_patterns      conversion.rs:221 — one arm of impl_int_from!,
#                                 where TryFrom<i64> for i64 has Error = Infallible
#   mismatched_lifetime_syntaxes  stack.rs:78, thread/thread.rs:206
# Kept here rather than in src/ so the audit diff against pristine 0.3.3 stays
# confined to two files. This silences these two lints only — a lint a future
# rustc adds still fires.
[lints.rust]
irrefutable_let_patterns = "allow"
mismatched_lifetime_syntaxes = "allow"

[package]
edition = "2021"
name = "piccolo"
version = "0.3.3"
authors = ["kyren <catherine@kyju.org>"]
description = "Stackless Lua VM implemented in pure Rust"
readme = "README.md"
license = "MIT"
repository = "https://github.com/kyren/piccolo"

[dependencies.ahash]
version = "0.8"

[dependencies.allocator-api2]
version = "0.2"

[dependencies.anyhow]
version = "1.0"

[dependencies.gc-arena]
version = "0.5.3"
features = [
    "allocator-api2",
    "hashbrown",
]

[dependencies.hashbrown]
version = "0.14"
features = ["raw"]

[dependencies.rand]
version = "0.8"
features = ["small_rng"]

[dependencies.thiserror]
version = "1.0"

[dev-dependencies.clap]
version = "4.5"
features = ["cargo"]

[dev-dependencies.rustyline]
version = "14.0"
